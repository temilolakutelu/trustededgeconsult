<?php

if (!defined('BASEPATH'))
    exit('No direct script access allowed');

class Subscription extends MY_Controller
{
    function __construct()
    {
        parent::__construct();
        $this->load->model(['Subscription_model', 'Audit_model']);
        $this->load->library('form_validation');
        $this->load->library('datatables');
    }

    public function index()
    {
        // $page_data = $this->Subscription_model->get_all();
        $page_data = $this->Subscription_model->get_all();

        $data = array(
            'title' => 'Subscriptions',
            'subscription_data' => $page_data,
        );

        $this->template->load('admin', 'subscription/list', $data);

    }

    public function create()
    {
        $data = array(
            'title' => 'Subscriptions',
            'button' => 'Add Subscription',
        );
        $this->template->load('admin', 'subscription/form', $data);
    }


    public function create_action()
    {
        $this->_rules();

        if ($this->form_validation->run() == false) {
            $this->create();
        } else {

            $data = array(
                'name' => $this->input->post('name', true),
                'email' => $this->input->post('email', true)
            );
            $this->Subscription_model->insert($data);
            // $task='Added '. $this->input->post('name',TRUE).' to Blood Group';
            $task = 'Added ' . $this->input->post('name') . ' to webpage table';
            $this->audit($task);
            $this->session->set_flashdata('message', 'Subscription created successfully');
            redirect(site_url('subscription'));
        }
    }


    public function edit($id)
    {
        $row = $this->Subscription_model->get_by_id($id);
        if ($row) {
            $data = array(
                'button' => 'Update',
                'title' => 'Update Subscription',
                'action' => site_url('subscription/edit_action'),
                'id' => set_value('id', $row->subscription_id),
                'name' => set_value('name', $row->name),
                'email' => set_value('email', $row->email),
            );
            $this->template->load('admin', 'subscription/edit', $data);
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('subscription'));
        }
    }

    public function edit_action()
    {
        $data = array(
            'name' => $this->input->post('name', true),
            'email' => $this->input->post('email', true)
        );
        $this->Subscription_model->update($this->input->post('id', true), $data);
        // $task='Updated '. $this->input->post('name',TRUE).' on Blood Group';
       // $this->audit($task);
        $task = 'Updated ' . $this->input->post('name') . ' page on webpage table';
        $this->audit($task);
        $this->session->set_flashdata('message', 'Subscription updated successfully');
        redirect(site_url('subscription'));
    }

    public function delete($id)
    {
      //  $this->check_role(); 
        $row = $this->Subscription_model->get_by_id($id);

        if ($row) {
            $this->Subscription_model->delete($id);
            $task = 'Deleted a subscription from Subscriptions table';
            $this->audit($task);
            $this->session->set_flashdata('message', 'subscription deleted Successfully');
            redirect(site_url('subscription'));
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('subscription'));
        }
    }

    public function audit($task)
    {
        $login = $this->session->userdata('logged_in');
        $user = $login['firstname'] . ' ' . $login['lastname'];
        $data_audit = array(
            'user' => $user,
            'task' => $task,
            'date_time' => date('Y-m-d H:i:s', time()),
        );

        $this->Audit_model->insert($data_audit);
    }

    public function _rules()
    {
        $this->form_validation->set_rules(
            'name',
            'name',
            'trim|required'
        );
        $this->form_validation->set_rules('email', 'Email', 'trim|required');
    }

}

/* End of file Blood_group.php */
/* Location: ./application/controllers/Blood_group.php */
/* Please DO NOT modify this information : */
/* Generated by Harviacode Codeigniter CRUD Generator 2017-06-01 14:53:53 */
/* http://harviacode.com */
